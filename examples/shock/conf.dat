&datum
 httx = 1,                      ! number of domains in x (converter)
 htty = 1,			 ! the same in y
 httz = 1,			 ! the same in z
 tindex_start = 0,               ! time slice to start at
 tindex_stop = 2026640,		 ! time slice to stop at. set to a large number to process all
 output_format = 1,		 ! output format. 2=file per slice, 1=all slices in one file
 append_to_files = 1 		 ! for cont. format set 1 to append to existing files, anything else to start at the beginning
$

