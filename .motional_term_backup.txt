SAVED motional term (physical-velocity conversion fix) from hyb_advance_e.cc E-macro.
Verified: reduces to working branch -u_phys x B on cartesian; needed because u=jf/rho is
contravariant (2/gd too big) and must be converted via H_i = h_i*gd_i/2 = H(i,0)*HGD(i).

To restore, replace the current motional line(s) in the E(x_,y_,z_) macro with:

    /* Motional -(u x B)_x, covariant. u=jf/rho is CONTRAVARIANT u^i; convert   \
       each component to physical velocity u_phys_i = u^i * H_i, H_i=h_i*gd_i/2 \
       (= H(i,0)*HGD(i)), then take the physical cross product with B (cb).      \
       On CARTESIAN u^i*H_i = u_phys_i and h=1 so this reduces to the working    \
       -u_phys_y*B_z + u_phys_z*B_y. */                                          \
    ( - (u##y_ * H(y_,0)*HGD(y_)) * Bf(0,z_)                                  \
      + (u##z_ * H(z_,0)*HGD(z_)) * Bf(0,y_) ) -                              \

(The HGD_x/HGD_y/HGD_z macros and hgdx/hgdy/hgdz = 0.5*g->dx/dy/dz remain defined;
they are only used by this term, so they can stay even while this is reverted.)
